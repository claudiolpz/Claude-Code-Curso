Tu meta es hacer un update a todas las dependencias vulnerables.

sigue esto:

1. Run `npm audit` para buscar paquetes vulnerables instalados en este proyecto
2. Run `npm audit fix` para aplicar updates
3. Run test y verifica que los updates no rompan nada